@model OfficeHour

@{
    ViewData["Title"] = "Confirm Delete";
}

<div class="row">
    <div class="col-md-6 offset-3">
        <p>Delete Office Hour</p>

        @if(TempData["Error"] != null)
        {
            <div class="col-md-12 alert alert-danger">
                <span><b>@TempData["Error"]</b></span>
            </div>
        }

        <div class="row">
            <div class="col-md-8 offset-2">
                <form asp-action="Delete">
                    @Html.AntiForgeryToken()
                    <div class="form-group">
                        <label asp-for="Day" class="control-label"></label>
                        <input asp-for="Day" class="form-control" disabled="disabled">
                    </div>

                    <div class="form-group">
                        <label asp-for="StartHour" class="control-label"></label>
                        <input asp-for="StartHour" class="form-control" disabled="disabled">
                    </div>

                    <div class="form-group">
                        <label asp-for="EndHour" class="control-label"></label>
                        <input asp-for="EndHour" class="form-control" disabled="disabled">
                    </div>

                    <div class="form-group">
                        <input type="hidden" name="id" value="@Model.Id">
                        <input type="submit" value="Confirm" class="btn btn-outline-success float-right">
                        <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>